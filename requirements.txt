# MeshBot - Meshtastic-Llama Bot Requirements
# Python 3.8+ required

# === Core Dependencies ===
# Meshtastic communication
meshtastic>=2.2.0

# Meshtastic dependencies
pyserial>=3.5
bleak>=0.22.3,<0.23.0
pyyaml>=6.0.1,<7.0.0
tabulate>=0.9.0,<0.10.0

# Message pub/sub system
pypubsub>=4.0.3

# HTTP requests
requests>=2.31.0

# === Telegram Integration ===
# Telegram bot API
python-telegram-bot>=21.0

# === AI & External Services ===
# Llama.cpp client uses requests (already listed above)

# === Weather & Alerts ===
# French weather vigilance alerts (web scraping)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# === Lightning Detection (Blitzortung) ===
# MQTT client for real-time lightning data
paho-mqtt>=2.1.0

# Geohash encoding for geographic filtering
pygeohash>=3.2.0

# === Encryption/Decryption ===
# AES encryption for decrypting Meshtastic packets
cryptography>=41.0.0

# NaCl/libsodium for MeshCore DM decryption (Curve25519 + XSalsa20-Poly1305)
PyNaCl>=1.5.0

# === MeshCore Support (Optional) ===
# Official MeshCore Python library for MeshCore companion mode
# Install only if using MeshCore companion mode (MESHCORE_ENABLED=True)
# meshcore>=0.1.0

# === Optional Dependencies ===
# For development/debugging
# ipython>=8.0.0

# === Notes ===
# Some packages require system dependencies:
# - pygeohash requires: python3-dev (or python3.X-dev for specific Python version)
#   Install with: sudo apt-get install python3-dev
#
# Installation:
#   pip install -r requirements.txt
#
# Or with --break-system-packages on managed systems (like Raspberry Pi OS):
#   pip install -r requirements.txt --break-system-packages
