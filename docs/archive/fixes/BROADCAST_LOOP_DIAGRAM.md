# Broadcast Loop Fix - Flow Diagram

## BEFORE FIX (Problem)

```
┌──────────────────────────────────────────────────────────────────┐
│                        USER SENDS /rain                          │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Bot receives (from=user_id, to=BROADCAST)                       │
│  ➜ on_message() called                                          │
│  ➜ is_from_me = (user_id == bot_id) → FALSE                     │
│  ➜ Process message                                               │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  handle_weather() generates response                             │
│  Response: "40da: 🌧️ Paris..."                                  │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  _send_broadcast_via_tigrog2()                                   │
│  ➜ broadcast_message(tigrog2, "40da: 🌧️ Paris...")             │
│  ➜ TCP connection 1 opens                                        │
│  ➜ Message sent FROM tigrog2_id                                  │
│  ➜ TCP connection 1 closes ✅                                    │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Mesh network propagates broadcast                               │
│  Bot receives (from=tigrog2_id, to=BROADCAST)                    │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ❌ Bot receives OWN broadcast                                   │
│  ➜ on_message() called AGAIN                                    │
│  ➜ is_from_me = (tigrog2_id == bot_id) → FALSE ⚠️              │
│  ➜ Process message AGAIN ⚠️                                     │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ❌ handle_weather() called AGAIN                                │
│  ❌ Generates SAME response AGAIN                                │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ❌ _send_broadcast_via_tigrog2() AGAIN                          │
│  ❌ broadcast_message(tigrog2, "40da: 🌧️ Paris...")             │
│  ❌ TCP connection 2 attempts to open                            │
│  ❌ TIMEOUT after 30 seconds ⏱️                                  │
│  ❌ ERROR: "Timed out waiting for connection completion"         │
└───────────────────────────────────────────────────────────────────┘

RESULT: Error, duplicate messages, wasted bandwidth
```

## AFTER FIX (Solution)

```
┌──────────────────────────────────────────────────────────────────┐
│                        USER SENDS /rain                          │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Bot receives (from=user_id, to=BROADCAST)                       │
│  ➜ on_message() called                                          │
│  ➜ is_from_me = FALSE                                           │
│  ➜ _is_recent_broadcast("rain") → FALSE                         │
│  ➜ Process message                                               │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  handle_weather() generates response                             │
│  Response: "40da: 🌧️ Paris..."                                  │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ✨ _send_broadcast_via_tigrog2()                                │
│  ✨ NEW: broadcast_tracker("40da: 🌧️ Paris...")                │
│     ➜ hash = md5("40da: 🌧️ Paris...")                          │
│     ➜ _recent_broadcasts[hash] = timestamp                       │
│     ➜ Tracked! 🔖                                                │
│  ➜ broadcast_message(tigrog2, "40da: 🌧️ Paris...")             │
│  ➜ TCP connection 1 opens                                        │
│  ➜ Message sent FROM tigrog2_id                                  │
│  ➜ TCP connection 1 closes ✅                                    │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Mesh network propagates broadcast                               │
│  Bot receives (from=tigrog2_id, to=BROADCAST)                    │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ✅ Bot receives OWN broadcast                                   │
│  ➜ on_message() called                                          │
│  ➜ is_from_me = FALSE (tigrog2_id ≠ bot_id)                     │
│  ✨ _is_recent_broadcast("40da: 🌧️ Paris...") → TRUE ✅        │
│     ➜ hash = md5("40da: 🌧️ Paris...")                          │
│     ➜ hash found in _recent_broadcasts                           │
│     ➜ age < 60s                                                  │
│  ✅ Message FILTERED (not processed) 🔄                          │
│  ✅ RETURN early                                                 │
└──────────────────────┬───────────────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│  ✅ Loop prevented!                                              │
│  ✅ No second TCP connection                                     │
│  ✅ No timeout error                                             │
│  ✅ Clean execution                                              │
└───────────────────────────────────────────────────────────────────┘

RESULT: Success, single message, no errors ✅
```

## Key Components

### Hash Tracking (60s window)
```
_recent_broadcasts = {
    "39c5a8b6...": 1700000000.123,  # "40da: 🌧️ Paris..."
    "bef00be7...": 1700000005.456,  # "40da: 🌤️ Londres..."
    ...
}
```

### Check Flow
```python
# Track before sending
hash = md5(message)
_recent_broadcasts[hash] = time.time()

# Check on receive
if message_hash in _recent_broadcasts:
    if age < 60:
        return True  # Filter
return False  # Process
```

## Timeline Comparison

### BEFORE (With Loop)
```
T+0s   : User sends /rain
T+0s   : Bot processes
T+0s   : Bot sends via tigrog2
T+3s   : TCP connection closes
T+3s   : Bot receives own broadcast
T+3s   : Bot processes AGAIN (loop!)
T+3s   : Attempts second TCP connection
T+33s  : TCP timeout ERROR ❌
```

### AFTER (With Fix)
```
T+0s   : User sends /rain
T+0s   : Bot processes
T+0s   : Bot tracks response hash 🔖
T+0s   : Bot sends via tigrog2
T+3s   : TCP connection closes
T+3s   : Bot receives own broadcast
T+3s   : Hash recognized, message filtered ✅
T+3s   : Done! No error ✅
```

## Memory Profile

```
Per broadcast tracked:
- Hash (32 chars):  64 bytes
- Timestamp (float):  8 bytes
- Dict overhead:    ~30 bytes
-----------------------------------
Total per entry:   ~100 bytes

Typical usage:
- 10 broadcasts/hour
- 60s window
- Max ~2-3 concurrent entries
- Memory usage: <1 KB
```
