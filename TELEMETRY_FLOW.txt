╔══════════════════════════════════════════════════════════════════════════╗
║                    ESPHOME TELEMETRY BROADCAST FLOW                      ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│                          INITIALIZATION                                  │
└─────────────────────────────────────────────────────────────────────────┘

    config.py
    ├── ESPHOME_TELEMETRY_ENABLED = True
    └── ESPHOME_TELEMETRY_INTERVAL = 3600  # 1 hour

    main_bot.py::__init__()
    └── self._last_telemetry_broadcast = 0

┌─────────────────────────────────────────────────────────────────────────┐
│                        PERIODIC TIMER (Every 5 min)                      │
└─────────────────────────────────────────────────────────────────────────┘

    periodic_update_thread()
    │
    ├── Sleep: NODE_UPDATE_INTERVAL (300s)
    │
    ├── Update node database
    ├── Cleanup old data
    ├── Save statistics
    │
    └── ⏰ TELEMETRY CHECK
        │
        ├── Is ESPHOME_TELEMETRY_ENABLED? ──→ No ──→ Skip
        │                                   │
        │                                   └→ Yes
        │
        ├── Time since last broadcast? ──→ < 3600s ──→ Skip
        │                                │
        │                                └→ >= 3600s
        │
        └── ✅ Call send_esphome_telemetry()

┌─────────────────────────────────────────────────────────────────────────┐
│                      TELEMETRY BROADCAST PROCESS                         │
└─────────────────────────────────────────────────────────────────────────┘

    send_esphome_telemetry()
    │
    ├── 1️⃣ Fetch Sensor Data
    │   │
    │   └── esphome_client.get_sensor_values()
    │       │
    │       ├── HTTP: GET /sensor/bme280_temperature ──→ 21.5°C
    │       ├── HTTP: GET /sensor/bme280_pressure ────→ 1013.25 hPa
    │       ├── HTTP: GET /sensor/bme280_humidity ────→ 56.4%
    │       └── HTTP: GET /sensor/battery_voltage ────→ 12.8V
    │       │
    │       └── Return: {
    │               'temperature': 21.5,
    │               'pressure': 101325.0,  # Auto-converted to Pa
    │               'humidity': 56.4,
    │               'battery_voltage': 12.8
    │           }
    │
    ├── 2️⃣ Create Telemetry Message
    │   │
    │   └── telemetry_pb2.Telemetry()
    │       │
    │       ├── time = 1700000000
    │       │
    │       ├── environment_metrics
    │       │   ├── temperature = 21.5
    │       │   ├── barometric_pressure = 101325.0
    │       │   └── relative_humidity = 56.4
    │       │
    │       └── device_metrics
    │           ├── voltage = 12.8
    │           └── battery_level = 64  # Calculated from voltage
    │
    └── 3️⃣ Broadcast to Mesh
        │
        └── interface.sendData(
                telemetry_data,
                destinationId = 0xFFFFFFFF,  # All nodes
                portNum = TELEMETRY_APP,
                wantResponse = False
            )

┌─────────────────────────────────────────────────────────────────────────┐
│                         NETWORK PROPAGATION                              │
└─────────────────────────────────────────────────────────────────────────┘

    Bot Node (tigrobot)
         │
         │ 📡 TELEMETRY_APP broadcast
         │
         ├──────────────┬──────────────┬──────────────┐
         │              │              │              │
         ▼              ▼              ▼              ▼
    Node A         Node B         Node C         Node D
    (tigrog2)     (User 1)       (User 2)      (Router)
         │              │              │              │
         │              │              │              └── Repeats broadcast
         │              │              │
         └── All nodes receive and can display telemetry

┌─────────────────────────────────────────────────────────────────────────┐
│                         RECEIVING NODES                                  │
└─────────────────────────────────────────────────────────────────────────┘

    Meshtastic Apps (iOS/Android/Web)
    │
    ├── Node Details → Telemetry Tab
    │   │
    │   ├── 🌡️ Temperature: 21.5°C
    │   ├── 🔽 Pressure: 1013.25 hPa
    │   ├── 💧 Humidity: 56.4%
    │   └── 🔋 Battery: 12.8V (64%)
    │
    └── Can plot graphs, set alerts, etc.

    Python API
    │
    └── pub.subscribe(on_telemetry, "meshtastic.receive.telemetry")
        │
        └── def on_telemetry(packet, interface):
                decoded = packet['decoded']
                telemetry = decoded['telemetry']
                env = telemetry['environmentMetrics']
                print(f"Temp: {env['temperature']}°C")

┌─────────────────────────────────────────────────────────────────────────┐
│                         ERROR HANDLING                                   │
└─────────────────────────────────────────────────────────────────────────┘

    Scenario 1: ESPHome Offline
    │
    └── get_sensor_values() ──→ Returns None
        │
        └── send_esphome_telemetry() ──→ Logs warning, no broadcast

    Scenario 2: Some Sensors Missing
    │
    └── get_sensor_values() ──→ Returns partial data
        │                         {'temperature': 21.5,
        │                          'pressure': None,
        │                          'humidity': None,
        │                          'battery_voltage': 12.8}
        │
        └── send_esphome_telemetry() ──→ Broadcasts available data only

    Scenario 3: All Sensors Missing (but ESPHome online)
    │
    └── get_sensor_values() ──→ Returns None (all values None)
        │
        └── send_esphome_telemetry() ──→ Logs warning, no broadcast

┌─────────────────────────────────────────────────────────────────────────┐
│                           CONFIGURATION                                  │
└─────────────────────────────────────────────────────────────────────────┘

    To Enable (default):
        ESPHOME_TELEMETRY_ENABLED = True
        ESPHOME_TELEMETRY_INTERVAL = 3600  # Every hour

    To Change Frequency:
        ESPHOME_TELEMETRY_INTERVAL = 1800  # Every 30 minutes
        ESPHOME_TELEMETRY_INTERVAL = 7200  # Every 2 hours

    To Disable:
        ESPHOME_TELEMETRY_ENABLED = False

┌─────────────────────────────────────────────────────────────────────────┐
│                         PERFORMANCE METRICS                              │
└─────────────────────────────────────────────────────────────────────────┘

    Network Impact:
        • 1 packet per hour (default)
        • ~100-200 bytes per packet
        • Broadcast (single transmission)

    Memory Impact:
        • Temporary allocation: ~1KB
        • Immediately garbage collected
        • No persistent storage

    CPU Impact:
        • HTTP requests: ~100ms total
        • Protobuf creation: <1ms
        • Runs in existing thread

    Total Overhead:
        • Negligible (<0.1% CPU)
        • No memory accumulation
        • Minimal network usage

╔══════════════════════════════════════════════════════════════════════════╗
║                              END OF FLOW                                 ║
╚══════════════════════════════════════════════════════════════════════════╝
