# Visual Guide: Meshtastic 2.7.15 DM Encryption

## 📊 The Issue Visualized

```
┌─────────────────────────────────────────────────────────┐
│  BEFORE: What You're Seeing                             │
└─────────────────────────────────────────────────────────┘

User sends DM: "/help"
    │
    ▼
┌─────────────────────────┐
│ Meshtastic Node (2.7.15)│
│ Encrypts with PKI       │
└───────────┬─────────────┘
            │ (Encrypted over LoRa)
            ▼
┌─────────────────────────────────────────────┐
│ Bot receives encrypted packet               │
│ ❌ Meshtastic library can't decrypt        │
│ ❌ Missing sender's public key!             │
└─────────────────────────────────────────────┘
            │
            ▼
[DEBUG] 🔐 Encrypted DM from 0xa76f40da to us
[DEBUG] ❌ Missing public key for sender
```

```
┌─────────────────────────────────────────────────────────┐
│  AFTER: How It Should Work                              │
└─────────────────────────────────────────────────────────┘

Step 1: Key Exchange (ONE TIME)
    │
    ▼
┌─────────────────────────┐        ┌──────────────────┐
│ Sender Node (2.7.15)    │◄──────►│  Bot Node       │
│ Broadcasts NODEINFO     │  PKI   │  Receives keys  │
│ with public key         │  Keys  │                 │
└─────────────────────────┘        └──────────────────┘
            │
            │ Keys exchanged ✅
            ▼
Step 2: DM Communication Works
    │
User sends DM: "/help"
    │
    ▼
┌─────────────────────────┐
│ Meshtastic Node (2.7.15)│
│ Encrypts with bot's     │
│ public key              │
└───────────┬─────────────┘
            │ (Encrypted over LoRa)
            ▼
┌─────────────────────────────────────────────┐
│ Bot receives encrypted packet               │
│ ✅ Meshtastic library decrypts with bot's  │
│    private key (has sender's public key)    │
│ ✅ Bot processes "/help" command            │
└─────────────────────────────────────────────┘
            │
            ▼
Bot responds: "Available commands: /bot, /nodes..."
```

## 🔑 Key Exchange Process

```
┌──────────────────────────────────────────────────────┐
│ Automatic Key Exchange (Every 15-30 minutes)         │
└──────────────────────────────────────────────────────┘

┌──────────────┐                      ┌──────────────┐
│ Node A       │                      │ Node B (Bot) │
│ (Sender)     │                      │              │
└──────┬───────┘                      └──────┬───────┘
       │                                     │
       │ Broadcasts NODEINFO_APP             │
       │ (includes public key)               │
       ├─────────────────────────────────────►
       │                                     │
       │                                     │ Stores A's key
       │                                     │ in node database
       │                                     │
       │            Broadcasts NODEINFO_APP  │
       │            (includes public key)    │
       ◄─────────────────────────────────────┤
       │                                     │
Stores B's key                               │
in node database                             │
       │                                     │
       ▼                                     ▼
   ✅ Ready                              ✅ Ready
   Can encrypt                           Can encrypt
   DMs to B                              DMs to A
```

```
┌──────────────────────────────────────────────────────┐
│ Manual Key Request (Immediate)                       │
└──────────────────────────────────────────────────────┘

┌──────────────┐                      ┌──────────────┐
│ Node B (Bot) │                      │ Node A       │
│              │                      │ (Sender)     │
└──────┬───────┘                      └──────┬───────┘
       │                                     │
       │ Request: meshtastic                 │
       │ --request-telemetry                 │
       │ --dest a76f40da                     │
       ├─────────────────────────────────────►
       │                                     │
       │                                     │ Immediately sends
       │                                     │ NODEINFO_APP
       │                                     │
       │            NODEINFO_APP response    │
       │            (includes public key)    │
       ◄─────────────────────────────────────┤
       │                                     │
Stores A's key                               │
immediately                                  │
       │                                     │
       ▼                                     │
   ✅ Ready in seconds                      │
   Can decrypt DMs                           │
   from A now                                │
```

## 🛠️ Solution Workflow

```
┌────────────────────────────────────────────────────────────┐
│ Step-by-Step Fix                                           │
└────────────────────────────────────────────────────────────┘

1️⃣ Diagnose
   │
   ▼
┌─────────────────────────┐
│ /keys tigro             │ ← Run on bot (mesh or Telegram)
│ or                      │
│ /keys a76f40da          │
└───────────┬─────────────┘
            │
            ▼
┌───────────────────────────────────────────┐
│ Output:                                   │
│ ❌ tigro t1000E: Pas de clé publique    │
│                                           │
│ 💡 Solution:                              │
│    Request NODEINFO from node             │
└───────────────────────────────────────────┘

2️⃣ Request Key
   │
   ▼
┌─────────────────────────────────────────┐
│ On bot's host machine:                  │
│ meshtastic --request-telemetry \        │
│            --dest a76f40da              │
└───────────┬─────────────────────────────┘
            │
            │ (Waits 2-5 seconds)
            ▼

3️⃣ Verify
   │
   ▼
┌─────────────────────────┐
│ /keys tigro             │ ← Check again
└───────────┬─────────────┘
            │
            ▼
┌───────────────────────────────────────────┐
│ Output:                                   │
│ ✅ tigro t1000E: Clé OK (a1b2c3d4...)   │
│                                           │
│ ✅ Vous POUVEZ:                           │
│    • Recevoir des DM de ce nœud          │
└───────────────────────────────────────────┘

4️⃣ Test DM
   │
   ▼
┌─────────────────────────┐
│ Sender sends: /help     │
└───────────┬─────────────┘
            │
            ▼
┌───────────────────────────────────────────┐
│ Bot logs:                                 │
│ [DEBUG] 📦 TEXT_MESSAGE_APP               │
│         de tigro t1000E f40da [direct]    │
│         (SNR:12.0dB)                      │
│                                           │
│ Bot responds with help text               │
└───────────────────────────────────────────┘

✅ SUCCESS!
```

## 🔍 Diagnostic Command Comparison

```
┌──────────────────────────────────────────────────────────┐
│ /keys Command Output Formats                             │
└──────────────────────────────────────────────────────────┘

MESH FORMAT (Compact, ≤180 chars):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/keys
🔑 2/3 avec clés. 1 sans.

/keys tigro
❌ tigro: Pas de clé publique

/keys deadbeef
✅ deadbeef: Clé OK (z9y8x7w6...)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


TELEGRAM/CLI FORMAT (Detailed):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
/keys
🔑 État des clés publiques PKI

Total nœuds: 3
✅ Avec clé publique: 2
❌ Sans clé publique: 1

⚠️ Nœuds sans clé publique:
   (Vous ne pouvez pas recevoir leurs DM)

   • tigro t1000E (0xa76f40da)

💡 Les clés s'échangent automatiquement via
   les paquets NODEINFO (broadcasts périodiques)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

/keys tigro
🔑 État des clés pour: tigro t1000E
   Node ID: 0xa76f40da

❌ Clé publique: MANQUANTE

⚠️ Vous NE POUVEZ PAS:
   • Recevoir des DM de ce nœud
   • Les DM apparaîtront comme ENCRYPTED

💡 Solution:
   1. Attendre l'échange automatique de clés
   2. Demander un NODEINFO au nœud:
      meshtastic --request-telemetry --dest a76f40da
   3. Vérifier que le nœud est en 2.5.0+
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## 📈 Before & After Comparison

```
┌──────────────────────────────────────────────────────────┐
│ BEFORE: Issue State                                      │
└──────────────────────────────────────────────────────────┘

DM sent ──► ❌ Encrypted ──► ❌ Not processed
                │
                └─► Logs: "🔐 Encrypted DM from..."
                └─► User confused
                └─► No diagnostic tools


┌──────────────────────────────────────────────────────────┐
│ AFTER: With This Solution                                │
└──────────────────────────────────────────────────────────┘

DM sent ──► ❌ Encrypted ──► ✅ Clear diagnosis
                │
                ├─► Logs: "❌ Missing public key"
                │         "💡 Solution: Request NODEINFO"
                │
                ├─► Command: /keys tigro
                │         Shows: "❌ Pas de clé publique"
                │         Suggests: "meshtastic --request-telemetry"
                │
                └─► User: Requests NODEINFO
                         └─► Keys exchanged
                              └─► DMs work! ✅
```

## 🎓 Understanding PKI Encryption

```
┌──────────────────────────────────────────────────────────┐
│ OLD WAY (Pre-2.7.15): PSK Encryption                     │
└──────────────────────────────────────────────────────────┘

Everyone uses SAME shared password
        │
        ▼
┌───────────────────────────────┐
│ Channel PSK: "AQ=="           │
│ (Shared by all nodes)         │
└───────────┬───────────────────┘
            │
            ▼
⚠️ PROBLEM: Anyone with channel password
           can read ALL DMs
           (Low security)


┌──────────────────────────────────────────────────────────┐
│ NEW WAY (2.7.15+): PKI Encryption                        │
└──────────────────────────────────────────────────────────┘

Each node has UNIQUE key pair
        │
        ▼
┌────────────────────────────────────────┐
│ Node A:                                │
│ • Private key: Secret (never shared)   │
│ • Public key: a1b2c3d4... (broadcast) │
│                                        │
│ Node B:                                │
│ • Private key: Secret (never shared)   │
│ • Public key: z9y8x7w6... (broadcast) │
└────────────┬───────────────────────────┘
             │
             ▼
✅ BENEFIT: Only Node B can read DMs to Node B
           Even if someone knows channel password
           (High security - End-to-End Encryption)
```

## 🚀 Quick Reference Card

```
┌─────────────────────────────────────────────────────┐
│ QUICK FIX COMMANDS                                  │
└─────────────────────────────────────────────────────┘

1. Check key status:
   /keys tigro

2. Request NODEINFO (if missing):
   meshtastic --request-telemetry --dest a76f40da

3. Verify success:
   /keys tigro

4. Test DM:
   (Sender) /help


┌─────────────────────────────────────────────────────┐
│ DIAGNOSTIC COMMANDS                                 │
└─────────────────────────────────────────────────────┘

/keys                  → Check all nodes
/keys <name>           → Check specific node
/info <name>           → Full node info
meshtastic --nodes     → List all nodes with keys (CLI)
meshtastic --info      → Show local node key (CLI)


┌─────────────────────────────────────────────────────┐
│ TROUBLESHOOTING STEPS                               │
└─────────────────────────────────────────────────────┘

1. Verify both nodes on 2.5.0+
2. Check key in BOTH directions (A→B and B→A)
3. Enable debug logs: DEBUG_MODE = True
4. Wait 30min for automatic exchange (or request manually)
5. See MESHTASTIC_2715_DM_ENCRYPTION_GUIDE.md
```

---

**Created**: 2024-12-24  
**For Issue**: Encrypted DM in Meshtastic 2.7.15  
**Solution**: `/keys` diagnostic command + enhanced logging  
**Status**: ✅ Complete and tested
