#!/usr/bin/env python3
"""
Exemple de configuration pour mode TCP
Configuration pour un node Meshtastic ROUTER accessible en réseau (WiFi/Ethernet)

Ce mode est utile pour:
- Placer le node à un emplacement optimal (extérieur, hauteur)
- Utiliser un node ROUTER existant
- Éviter les câbles USB entre le Pi et le node
"""

# ========================================
# MODE DE CONNEXION - TCP
# ========================================
CONNECTION_MODE = 'tcp'

# Adresse IP du node Meshtastic
# Le node doit être configuré en mode ROUTER avec WiFi/Ethernet activé
TCP_HOST = "192.168.1.38"  # Remplacer par l'IP de votre node
TCP_PORT = 4403            # Port TCP par défaut de Meshtastic

# Pour trouver l'IP de votre node:
# 1. Via l'application Meshtastic: Settings > Network > WiFi > IP Address
# 2. Via votre routeur: chercher "Meshtastic" dans les appareils connectés
# 3. Via nmap: nmap -p 4403 192.168.1.0/24

# Pour tester la connexion:
# nc -zv 192.168.1.38 4403

# ========================================
# CONFIGURATION LLAMA.CPP
# ========================================
LLAMA_HOST = "127.0.0.1"
LLAMA_PORT = 8080

# ========================================
# CONFIGURATION TELEGRAM (OPTIONNEL)
# ========================================
TELEGRAM_ENABLED = True
TELEGRAM_BOT_TOKEN = "YOUR_TELEGRAM_BOT_TOKEN_HERE"
TELEGRAM_AUTHORIZED_USERS = []  # Vide = tous autorisés

# ========================================
# AUTRES PARAMÈTRES
# ========================================
# Voir config.py.sample pour la liste complète des options
DEBUG_MODE = False

# ========================================
# NOTES IMPORTANTES MODE TCP
# ========================================
# 1. Le node doit être en mode ROUTER (ou ROUTER_CLIENT)
# 2. WiFi/Ethernet doit être activé et configuré sur le node
# 3. Le node doit avoir une IP fixe (DHCP reservation ou IP statique)
# 4. Le port 4403 doit être accessible (vérifier firewall)
# 5. La latence réseau peut affecter la réactivité du bot
